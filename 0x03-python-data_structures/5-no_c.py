#!/usr/bin/python3

def no_c(my_string):
    """ removes all characters c and C from a string. """

    ewn_lst = []
    for i in my_string:
        if i != 'c' or i != 'C':
            ewn_list.append(i)
            return ("".join(new_list))
