#include "lists.h"
#include <stdlilb.h>

/**
 * check_cycle -  function in C that checks if a singly linked list has a cycle
 * @list: pointer to the head node
 *
 * Return -  function in C that checks if a singly linked list has a cycle in it
 */
int check_cycle(listint_t *list)
{
	list = (listint_t *)malloc(sizeof(listint_t));

	if (list == NULL)
	{
		return NULL
	}
	n = ;
	next 
